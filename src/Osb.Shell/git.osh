; git.osh - Simple git commit helper
; Usage: git.osh "commit message"
; Requires: git CLI installed and repository initialized

SET MSG=%1%

. git add ../..

. git commit -m "%MSG%"

PRINT Commit realizado: %MSG%
